let x:number=10
function sum(x:number, y:number):number {
  return x + y
}
let a:string[]=["apple", "banana", "cherry"]
interface ICars{
    wheels?: number,
    color: string | number
}
let car:ICars={
    wheels:5,
    color:"red"
}